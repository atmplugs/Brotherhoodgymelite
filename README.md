# Brotherhood Gym — website

Live site files. Edit **index.html** only.

## Files
| File | Edit it? |
|---|---|
| `index.html` | Yes — all text lives here |
| `404.html` | Rarely |
| `logo.webp` | No — the crest |
| `clip.mp4` / `poster.jpg` | No — the training clip |
| `og-image.jpg` | No — preview picture when the link is shared |
| `.nojekyll` | No — leave it, it must stay |

## To change text
Click `index.html` -> pencil icon -> edit -> **Commit changes**. Live in ~1 minute.

## Still to do
- Wednesday class time (currently reads "Call for time")
- Replace `YOURDOMAIN.com` in the two `og:` lines once the domain is live

## Don't touch
Anything between `<style>` and `</style>`, or the `<script>` blocks at the bottom.
